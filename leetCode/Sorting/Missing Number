// Approach: Use XOR to cancel out all numbers and find the missing one
// Time Complexity: O(n)
// Space Complexity: O(1)

int missingNumber(int* nums, int numsSize) {
    int xor = numsSize;

    for (int i = 0; i < numsSize; i++) {
        xor ^= i ^ nums[i];
    }

    return xor;
}
